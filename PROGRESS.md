# ClaudeMod 開発進捗 (PROGRESS.md)

このファイルは、1時間ごとに自動起動される開発セッション間の**唯一の記憶**です。
新しいセッションを始める前に必ずこのファイル全体を読んでください。会話履歴は引き継がれません。

最終更新: 2026-08-31(直接チャットセッション、v0.33.1リリース: バグ修正依頼への対応 - 羽石のジャンプ時メッセージ連発、パルスチャーム/ウォードストーンのスライム/マグマキューブ未検知)

**このファイルの構成(2026-08-30に再整理)**: 以前は「セッションごとに実装内容を長文で追記し続ける」運用で肥大化していたため(ピーク時4000行超)、今回から以下の5分類に固定した。

1. 約束や決まり事 — 必ず遵守する恒久的なルール・技術的な決まり事
2. TODO — 次にやるべきことを優先度順(上ほど高い)に並べたリスト
3. 問題点 — 既知の不具合・未検証事項の箇条書き
4. その他 — 参考情報・議論したい論点・通知状況など
5. MOD構想・ロードマップ — MOD全体の方向性

**今後の運用ルール(重要)**: このファイルにセッションごとの実装経緯を長文で追記し続けない。新しい実装をしたら、TODO/問題点/その他を書き換える(該当項目を完了に更新 or 削除 or 新規追加)ことでこのファイル自体は常にコンパクトに保つ。経緯を残したい詳細な実装ログ・議論の記録は`PROGRESS_ARCHIVE.md`に追記すること。

過去の全セッション(#3〜#83)の詳細な実装ログは`PROGRESS_ARCHIVE.md`にある。「なぜこの実装になったか」を調べたい場合はそちらを参照すること。

---

## 1. 約束や決まり事(必ず遵守)

* **Git / push運用**
  * 作業ディレクトリは必ずユニークなパスを使う(`mktemp -d`、またはセッション専用ディレクトリ配下)。`git config user.name/user.email`は`ClaudeMod Session Agent <claudemod-agent@users.noreply.github.com>`に設定する。
  * `git push`はまず素の状態(プロキシ環境変数に手を加えない)で試す。「access denied by the git proxy」等で失敗した場合にのみ`https_proxy="" HTTPS_PROXY="" http_proxy="" HTTP_PROXY=""`を付けて再試行する(順序を逆にしない)。
  * 複数セッション同時実行に備え、push前に必ず`git fetch origin main`し、差分があれば`git rebase origin/main`する。

* **ビルド・CI確認 / 通知**
  * ローカルビルド(`./gradlew build`)はサンドボックス内では必ず失敗する(プロキシのallowlist制限)。ビルド確認はGitHub Actions経由のみ。
  * `api.github.com`への直接アクセスも不可なため、リポジトリにコミットされる`builds/last_datapack_validation_summary.txt`/`last_ore_verification.txt`/`last_datapack_validation_errors.log`で結果を確認する。
  * Discord Webhookへの直接送信もサンドボックスから不可。通知はGitHub Actions側(`build-and-notify.yml`/`release.yml`)に任せ、無駄なリトライをしない。
  * `.github/workflows/build-and-notify.yml`のNotify Discordステップは、pushに含まれる全コミットの件名(自動コミット除く)を箇条書きで送る仕様。ワークフローを触る際もこの挙動を壊さないこと。

* **GitHub Issue対応**
  * 毎回の状況確認にGitHub Issueの確認を含める。`https://github.com/Konpeitou24/ClaudeMod/issues`や個別issueページを非ログインで`curl`取得(キャッシュバスティング用クエリ`?nocache=$(date +%s%N)`を付ける)。コメント本文は`react-app.embeddedData`のJSON(`data['payload']['preloadedQueries'][0]['result']['data']['repository']['issue']`のパス)をパースして読む。
  * このgitトークンにはIssueへのコメント投稿・クローズ権限が無いため、`ISSUES_TO_CLOSE.json`/`PENDING_ISSUES.json`のリレー機構(`.github/workflows/`に整備済み)を使う。
  * 投稿者が`Konpeitou24`本人ならその場で対応してよい。それ以外の投稿者のIssueは`PENDING_ISSUES.json`に登録して保留する。
  * 【2026-08-31追記・教訓】Issue一覧の確認は「PROGRESS.mdのTODOに載っているissue番号だけ」ではなく、必ず`is:issue is:open`で全件検索すること。今回、#16(クロノフレイム)・#17(羽石)がそれぞれv0.6.0・v0.19.0時点で既にコード上完全対応済みだったにもかかわらず、PROGRESS.mdのTODOに載っていなかったせいで何セッションもクローズし忘れていたことが判明した。全件チェックしていれば早期に気付けていたはず。

* **コンテンツ制作(テクスチャー・音)**
  * テクスチャーは第三者の作品をコピーしないこと。Python(Pillow)でゼロから自作するのが基本だが、こんぺいとう氏本人が作成・提供したテクスチャー(例: 蒼白のプリズミウムブロックの合作、2026-08-30)や、Minecraft本体のバニラテクスチャーを土台に調整する手法も選択肢にしてよい(2026-08-30追記)。いずれの手法でも生成・改変後は必ず拡大画像を目視確認する。
  * 音声(サウンド)方針: バニラの`SoundEvent`を複数レイヤーする(音量・ピッチを変えて`playSound`を複数回呼ぶ)ことを常に第一候補とする。Python合成(numpy等)は、バニラに近い音が本当に存在しない場合に限る最終手段。
  * 新ブロック追加時は関連タグ(`mineable/pickaxe`、`walls`等)への登録漏れに注意する(過去に2回発生済み)。

* **コード・ファイル編集の制約**
  * lang(en_us.json/ja_jp.json)等の整形済みJSONを部分編集する際は、`json.load`+`json.dump`による全体再整形をしない。既存エントリの直後に新規行を文字列置換で挿入する方式を使う。
  * このセッション環境のRead/Write/Editツールはリポジトリのgit作業ディレクトリ(Linuxサンドボックス内パス)に直接使えないことが多い。ファイル編集は`mcp__workspace__bash`経由のpython/sed/catで行い、画像確認だけはWindows側マウントの作業フォルダ(outputs)にコピーしてから`Read`ツールで開く。
  * 外部API・Minecraft本体の未確認仕様を調べる際は`WebSearch`/`mcp__workspace__web_fetch`を積極的に使う。`minecraft.wiki`・`mappings.dev`(1.20.1 mojmap javadoc)等には到達できる(bashの`curl`は`api.github.com`等の主要ホストが到達不可)。それでも足りなければ`mcp__Claude_Browser__*`で直接リポジトリ・公式mavenを読む。

* **外部MOD連携**
  * Mixinベースの外部依存MOD(Curios等)を`compileOnly`/`runtimeOnly`で追加する場合、CIの`runGameTestServer`で実際にロードされクラッシュしうる。`build.gradle`の該当runブロックに`mixin.env.remapRefMap` / `mixin.env.refMapRemappingFile`の設定が必要になる場合がある。
  * 外部MODのスロット/インベントリ拡張機能に対応する際は、「スロット種別への登録・タグ付け」と「エンティティへのスロット配布」が別々の必須ステップであることを確認する(Curios対応で一度見落とし、実機テストで発覚し後日修正した実例あり)。

* **GUI/Menu実装**
  * このMODのGUI付きブロック(Menuクラスを持つもの)は、クライアント側のMenuコンストラクタで「その場にある本物のブロックエンティティのContainerData/インベントリをそのまま使い回す」実装にしないこと。Forge公式ドキュメント(https://docs.minecraftforge.net/en/1.20.1/gui/menus/)が明記する通り、クライアント側は常に新規のダミー`SimpleContainerData`/空の`ItemStackHandler`を使うのが正しい実装であり、本物のインスタンスを使い回すと(このMODのContainerDataは`set()`を意図的にno-opにしているため)サーバーからの同期パケットの値が握りつぶされ、GUIのバー表示が固まって見える(v0.31.2で発覚・修正、詳細はPROGRESS_ARCHIVE.md参照)。
  * ContainerDataの`get()`はサーバー側で常に生きているフィールドを直接読む実装で問題ないが、`set()`は「クライアント側が同期パケットを受け取った時にしか呼ばれない」ことを忘れないこと。`set()`をno-opにする設計自体は間違いではないが、クライアント側Menuコンストラクタがその本物のインスタンスを参照してしまうと同期が機能しなくなる。

* **ドキュメント運用(このファイル自体の扱い)**
  * セッションがPROGRESS.md更新前に終了する可能性がある。作業開始時、直近リリースタグ以降のコミットを`git log`で確認し、記録漏れが無いかチェックする。あれば遡って記録する。
  * `PROGRESS_ARCHIVE.md`に全セッション(#3〜#83)の詳細な実装ログがある。経緯を詳しく調べたい場合はそちらを参照すること。
  * **「今どんな機能が実装済みか」をざっと把握したい場合は、PROGRESS.md内に詳細な実装済み一覧を維持するのではなく`RELEASE_NOTES.md`を確認する手もある**(2026-08-30、こんぺいとう氏より: バージョンごとの追加内容が既にそこにまとまっているため)。
  * **【2026-08-30、こんぺいとう氏の指摘で発覚】PROGRESS.mdの記述、特に「5. MOD構想・ロードマップ」の技術的な説明は、実装が変わった後も更新されず古いまま残ることがある。** 実例: Prism Realmの地形は初期実装(session 14)ではバニラのオーバーワールド設定+固定バイオームcherry_groveを流用していたが、ユーザー指摘を受けて`minecraft:flat`型の水没ワールド+専用バイオーム`claudemod:prism_realm`+自作Perlin/Fractalノイズによる境界のまだら化、へと既に作り直し済み(session 30台)だったにもかかわらず、記述が古いまま残っていた。**ディメンション・worldgenなど「今どうなっているか」が重要な話題では、記述を鵜呑みにせず実際のデータパックJSON・Javaソースを確認してから発言・実装すること。**

---

## 2. TODO(優先度順、上ほど優先度が高い)

1. **【最優先・新規、2026-08-31直接チャットでの依頼】v0.33.1で、こんぺいとう氏から「今あるすべてのバグを直してほしい」との依頼を受け、GitHub Issue全件洗い出し(`is:issue is:open`)の上で2件のバグを修正した。次回、実機確認を依頼すること: (a) issue #17(羽石) - 通常のジャンプ・ダッシュではメッセージ・演出が出ず、3ブロックを超える危険な落下でのみ「75%軽減」表示が出るか、(b) プリズミウム・パルスチャームを使った時、近くのスライム・マグマキューブにも発光効果が付くか、(c) プリズミウム・ウォードストーンの近くにスライム・マグマキューブがいる場合も弱体効果が発動するか。**
2. **【重要・教訓】今回、GitHub Issueの`is:issue is:open`全件検索を久しぶりに実施したところ、issue #15(電力バグ)・#17(羽石、こんぺいとう氏により再オープン済み)がPROGRESS.mdのTODO/問題点のどちらにも載っていないことが判明した(以前解決したと思い込んで記載から漏れていた)。決まり事に「全件検索を徹底する」旨は既にあるが、それでも今回漏れていたため、次回以降は本項目のようにTODO自体にも「前回issue全件確認はいつ・結果どうだったか」を明示的に残すこと。**
3. Issue #15(電力についてのバグ)の残り: セル二重貯蓄・ケーブル多段リレー・GUI固まり・発電機インベントリ無しは過去セッション(v0.27.0〜v0.31.2、session 55〜58)で対応済みのはずだが、こんぺいとう氏からの実機確認がまだ取れていない。また「隣接消費ブロック設置時に生産側が一時的に0になる」という追加コメントは、コードレビューの結果「発電機の生成速度10FE/tickに対し消費側への押し出し上限200FE/tickの方が大きいため、バッファがすぐ0近くまで押し出される正常な仕様」と判断し、Generator画面に今何FE生成/押し出し中かを表示するUIを追加済み(`lastGenerated`/`lastPushed`)。次回、この説明と対応で納得いただけるか、まだ体感的に「合わなくなる」ように見えるかをこんぺいとう氏に確認すること。コメント中の「セルに発電機なしで直接充電できてしまう」はバグではなく設計上の論点として4.その他に記載。
4. **【最優先・新規】v0.31.2で「全GUIブロックの画面固まりバグ」を修正した(問題点参照)。修正内容はコードレビューとForge公式ドキュメントの記述に基づく(このサンドボックスでは実機確認不可)。次回、こんぺいとう氏に粉砕機/精錬機/圧縮機だけでなくPrismium Cell/Generator/Pylon/Restorer/WardstoneのGUIも含めて実機確認を依頼すること。直っていなければ最優先で再調査(その場合、今回の仮説=クライアント側Menuコンストラクタが本物のContainerDataを使い回していた、が的外れだった可能性が高いので振り出しに戻ること)。**
5. プリズミウム・クロノフレイムのGUI(v0.33.0)について、右クリックで画面が開くか・ボタンで時刻が変わるか・クールダウン表示が機能するか、こんぺいとう氏に実機確認を依頼すること。
6. 【新規・優先度を上げた】GameTestフレームワーク(このMODが既にCuriosAPI検証で使っている`runGameTestServer`基盤)で、ContainerDataの同期(クライアント側`set()`が実際に値を反映するか)を自動検証する仕組みを追加する。今回のバグが8ブロック全てに共通する設計ミスだったにもかかわらずCIビルド成功だけでは検知できなかった教訓を踏まえ、再発防止として優先度を上げた。
7. プリズミウム・クローラー(v0.31.0で追加した5体目のMOB)が実機で意図通り動作しているか(見た目・自然スポーン・鳴き声)、こんぺいとう氏に確認を依頼する。
8. Issue #18対応(Curios統合)の残りの実機確認。**【2026-08-30、こんぺいとう氏が実機確認済み】Guardian CharmはCurios対応済みで、右クリックでcharmスロットに装備できることを確認済み。** 残る未確認は、Featherstone/Emberguard/Vitastone/Magnet Charmの4アイテムのCurios GUIでのスロット表示・ドラッグ&ドロップ・右クリック装備、および全5アイテム共通で「装備中に効果が実際に発動するか」。これらの確認が取れ次第issue #18をクローズする。
9. Issue #21(JEI互換性)への着手方針を検討する。着手する場合は決まり事14(mixin remapRefMap)を思い出すこと。機能追加であり、こんぺいとう氏から「バグ修正が先、機能追加は後」と明確な指示があったため(2026-08-31)、既知のバグが片付くまで着手しないこと。
10. **【2026-08-30、こんぺいとう氏からの指摘で順序変更・記述修正】Prism Realmにまず「平原」の基本地形を追加する。** 現状のPrism Realmは`minecraft:flat`型のチャンクジェネレータによる「水没ワールド」で、バニラのオーバーワールド設定や固定バイオームは使っていない(過去に一度そういう実装をしてこんぺいとう氏から強く指摘され、既に修正済み。決まり事18参照)。専用バイオーム`claudemod:prism_realm`・自作のPerlin/Fractalノイズによるプリズミウムの深層岩/石の境界のまだら化(`PrismiumStoneTransitionFeature`)は実装済みだが、地形の起伏自体が無く地表全体が海になっている(陸地が存在しない)。この土台(陸地)が無いまま巨大山岳地帯のような特徴地形だけを追加すると、「海に急に山岳地帯がある」ような文脈のない不自然な地形になってしまう。
11. 上記10の後に、**各バイオームに固有のボスを伴うダンジョンが低確率で生成される仕組み**を実装する(こんぺいとう氏からの要望、2026-08-30:「平原のボス、山岳地帯のボス、みたいな感じで各バイオームにまれにダンジョンが生成されるようにしてほしい」)。ユーザー直接要望「Prism Realm巨大山岳地帯+ボス」は、この仕組みの山岳バイオーム版として最初に着手する。**10より先に着手しないこと**(海のような場所に唐突に山岳があると意味が分からなくなるため)。規模が大きいため、(a)バイオームごとのダンジョン構造生成、(b)バイオーム固有のボスMOB、(c)低確率でのダンジョン配置ロジック、に分割して段階的に進めること。山岳版が完成したら、平原など他バイオームにも同じ仕組みを横展開する。
12. MOBのカテゴリ拡充を検討する。現状「戦闘」「水中非戦闘」「地上アンビエント」の3種類があり、飛行アンビエントMOB・使い魔的MOB等の新カテゴリを追加する余地がある(バイオーム固有ボスもこの拡充の一環)。
13. 【2026-08-31、新規】v0.32.0で追加したプリズミウム・コンペンディウム(issue #7対応のガイドブック)について、次回こんぺいとう氏に実機確認を依頼すること。確認したい点: (a) 初回ワールド参加時に自動で1冊インベントリに入るか、(b) 本を開いた際に11ページ全て正常に表示され、日本語ページが本のページ内に収まりきっているか(はみ出していないか)、(c) クリエイティブタブにも表示されているか。
14. コンペンディウムを紛失した場合の再入手手段(クラフトレシピ等)がまだ無い。バニラの本+プリズミウムの欠片のような簡単なレシピで再発行できるようにする(専用NBTを持つカスタムレシピが必要、vanillaのBookCloningRecipeのような特殊レシピクラスの実装が要る)。
15. コンペンディウムの内容は現状11ページの概要のみ。今後、各エネルギー機械の詳細な配線図解や、Prism Realmのダンジョン/ボス実装(TODO10)が進んだらその案内ページを追加するなど、実装が進むたびに内容を拡充していく。
16. **【2026-08-31、新規】Prismium Lantern / Pale Prismium Lanternが、バニラの吊り下げ形状ではなく単純な立方体(`cube_all`)のままになっている問題(session 4から未着手、長期間放置)。今回のバグ修正セッションで見送った理由: 単なるモデル差し替えでは済まず、吊り下げ(hanging)判定・水没(waterlogged)対応・当たり判定の変更を伴うBlockクラスの作り直しが必要な「機能追加」寄りの変更であり、実機確認できないサンドボックスでリスクの高い変更を当てずっぽうで行うのは避けた。着手する場合は、バニラの`LanternBlock`のフィールド・当たり判定をどこまで踏襲するか事前に設計を固めてから着手すること。**

---

## 3. 問題点(既知の不具合・未検証事項)

- **【修正済み(v0.31.2)・実機未検証】全GUIブロック(Cell/Generator/Pylon/Restorer/Wardstone/Pulverizer/Smelter/Compressor)のGUIが開いても中身が固まって動作しない不具合の原因は、v0.27.0の3機械共通基底クラス化ではなく、セッション23の最初のGUI実装から存在していた「全Menuクラス共通の設計ミス」だったと判明した。** クライアント側Menuコンストラクタが「その場にある本物のブロックエンティティのContainerDataをそのまま使い回す」実装になっており、このMODのContainerDataは`set()`を意図的にno-opにしているため、クライアント側では同期パケットの値が握りつぶされてGUI表示が固まって見えていた(Forge公式ドキュメントが明記する「クライアント側は常にダミーのSimpleContainerDataを使うべき」に反する実装だった)。v0.31.2で全8ブロックのMenuクラスを修正済みだが、このサンドボックスでは実機確認ができないため、次回こんぺいとう氏の実機確認が必要(TODO1参照)。
- Issue #15(電力バグ)・#17(羽石、こんぺいとう氏により再オープン)・#18(CuriosAPI対応)・#21(JEI互換性)は引き続きOPEN。【2026-08-31、教訓】#15/#17はPROGRESS.mdへの記載漏れで長期間見落とされていた(TODO2参照)。#17は今回v0.33.1でジャンプ時のメッセージ連発を修正済み(実機未検証、TODO1参照)。#15は電力の二重貯蓄・ケーブル・GUI固まり・インベントリ無しは過去セッションで対応済みのはずだが実機未検証、「隣接消費ブロック設置時に生産側が一時的に0になる」は仕様と判断しUIで可視化(TODO3参照)。
- **大前提**: このサンドボックスは実機(ゲームクライアント)を起動できないため、MOD内のほぼ全コンテンツが「CIビルドが通ること」以上の検証が一切できていない。具体的にはバランス数値(FE容量・攻撃力・防御力・生成密度・クールダウン等)、装着時テクスチャー・インベントリ表示・GUI表示、FE配電経路が実際に繋がって動くか、全MOBの自然スポーン頻度・AI挙動、worldgen装飾ブロックの生成、全GUIの表示崩れ、サウンド/パーティクル演出のタイミングと音量感が丸ごと未検証。新しいコンテンツを追加するたびに、この一般則で説明できる「未検証」を個別に書き足す必要はない(個別に書く価値があるのは下記のような特殊な課題だけ)。
- Prismium Lanternがバニラの吊り下げ形状ではなく単純な立方体(`cube_all`)のまま(session 4から未着手、TODO16参照)。
- `ArmorSetBonusHandler`は`TickEvent.PlayerTickEvent`を毎tick・全プレイヤー分処理する。サーバー側限定ガードはあるが、プレイヤー数が多いサーバーでの負荷は未計測。
- **【修正済み(v0.33.1)・実機未検証】** `Monster`クラス限定でMobを走査する実装(Prismium Wardstone、Prismium Pulse Charm)は`Slime`/`MagmaCube`(`Mob`は継承するが`Monster`は継承しない)を取りこぼしていた。`Mob.class`を`instanceof Enemy`でフィルタする方式に変更し、両方とも検知できるようにした(1.20.1マッピングで`Slime implements Enemy`を確認済み)。実機での効果発動確認はまだ取れていない(TODO1参照)。
- `ResourceLocation`/`FMLJavaModLoadingContext`の非推奨API警告は1.20.1では有効な置き換え先が無い(1.21系に上げない限り対応不能、今後このタスクを申し送りに書かないこと)。
- セッションがPROGRESS.md更新前に終了するリスクが実際に一度発生した(セッション#81、git履歴から遡って復元済み)。今後も作業開始時に必ず確認すること(決まり事16)。
- 【2026-08-31、新規・未検証】プリズミウム・コンペンディウム(v0.32.0)は、`PlayerLoggedInEvent`で初回ログイン時に自動配布する設計。このイベントがシングルプレイ/マルチプレイ双方で意図通り一度だけ(ワールドを跨いでプレイヤーUUIDごとに一度だけ)発火するか、既存プレイヤーのセーブデータに新しいNBTフラグを安全に追加できるかは実機未検証。
- 【2026-08-31、新規】プリズミウム・フェザーストーン(羽石)は、通常のジャンプでも落下距離>0であるため毎回メッセージ・演出が発火していた(issue #17再オープンの原因)。v0.33.1で、実際にダメージが発生する落下(3ブロック超)の時のみ発動するよう修正した。実機未検証(TODO1参照)。
- 【2026-08-31、新規・未検証・重要】プリズミウム・クロノフレイム(v0.33.0)を「時計を持って右クリック」方式から「右クリックでGUIを開き、ボタンで操作」方式に全面刷新した。このMOD初のBlockEntityを持たないメニュー(`SimpleMenuProvider`ベース)であり、このMOD初の`clickMenuButton`(ボタンクリックをサーバーに伝える標準Minecraft機構、バニラのビーコン/織機と同じ仕組み)の使用でもある。既存の8GUI(ContainerData同期のみ、プレイヤー操作なし)とは構造が異なるため、v0.31.2のGUI固まりバグと同じ原因では起こり得ない別種の不具合(画面自体が開かない、ボタンを押しても反応しない等)が起きるリスクがある。実機確認が最優先(TODO1参照)。

---

## 4. その他

**使えるもの**: JDK 21がプリインストール(JDK 17は`apt-get install openjdk-17-jdk-headless`で追加可能)。システムGradle 8.14.3が`/opt/gradle`にプリインストール済み。Python3 + Pillowはテクスチャ生成に使用可能。`github.com`(`api.github.com`は不可)、`raw.githubusercontent.com`は到達可能。

**議論したい論点・改善案**:
- Pulse Charm(右クリックで能動的に使うアイテム)は意図的にCuriosのcharmスロット対象外にしたままだが、この判断への同意が得られるか、あるいは別のアプローチ(右クリックでなくCurios経由の常時発動効果に作り替える等)を検討すべきか。**Guardian Charmは対象外にする案だった(§3CL)が、後日v0.30.1で対応拡張済み・実機でも動作確認済み(TODO5参照)なので、この論点はPulse Charmのみが対象。**
- ポータルフレームの専用ブロック化・6個セット化案。要望があれば着手を検討。
- Prismium Ingot/Alloy Ingotのスミシングアップグレード経路の再検討。
- `PROGRESS_ARCHIVE.md`への分離運用(2026-08-30に本格導入)が今後も機能するか、数セッション後に振り返りたい。
- issue #7(MODの説明不足)は、v0.32.0のプリズミウム・コンペンディウムで大きく前進したが、完全クローズにはまだ早いと判断(実機未検証、かつ「ゲーム内で知ることができない」という報告の核心にはまだ再入手手段が無い等の穴がある)。実機確認とTODO14(再入手レシピ)が済んでからクローズを検討する。
- 【2026-08-31、新規】issue #15のコメントで「セルなどに発電機なしで直接充電できてしまうのは見た目よろしくない」との指摘があった。現状は仕様(FE capabilityを持つブロックはどこからでも受電できるのが一般的な設計)だが、こんぺいとう氏がこれをバグ/改善要望とみなすなら、Cellの受電を「発電機経由のみ」に制限する設計変更を検討する余地がある。バグではなく設計判断の論点として保留。

**通知状況**: Discord Webhookはサンドボックスから到達不可のため試みていない。GitHub Actions側(`build-and-notify.yml`・`release.yml`)がpush/タグに対応する通知を送信する(Secret設定済み前提)。

**2026-08-31追記**: このv0.33.0は定期実行セッションではなく、こんぺいとう氏との直接チャットセッション中に実装した(前段のv0.32.0は同日の定期実行セッション)。直接チャットセッションであっても、次の定期実行セッションが状況を正しく引き継げるようこのPROGRESS.md/HANDOFF.mdは通常通り更新している。

**2026-08-31追記(v0.33.1)**: 同日、こんぺいとう氏より「今あるすべてのバグを直してほしい、機能追加はその後」との直接チャット依頼があり、GitHub Issueの`is:issue is:open`全件洗い出しから着手した。その結果、issue #15/#17がPROGRESS.mdに記載漏れだったことが判明(TODO2の教訓参照)。今回は実装レベルで明確に再現・修正できた2件(#17のジャンプ時メッセージ連発、Pulse Charm/Wardstoneのスライム/マグマキューブ未検知)を修正しv0.33.1としてリリース。#15の残り論点や、モデル作り直しが要るPrismium Lantern形状(TODO16)は、実機確認できないサンドボックスで当てずっぽうに手を広げるリスクを避け、今回はスコープ外とした。

---

## 5. MOD構想・ロードマップ

「てんこ盛り」コンテンツMODとして、以下の柱を段階的に育てていく。優先順位や詳細は毎回のセッションで見直してよい。「完成」を目指さず、常に肉付けし続ける。各要素は最初は最小実装で入れて、後のセッションで機能・バランス・ビジュアルを磨き込む前提。

1. **新資源・素材ライン**: Prismium(プリズミウム) — 最初の資源で、装備・エネルギー・ディメンションの共通テーマ素材。派生として「蒼白のプリズミウム」(Pale Prismium)ファミリーも展開中(装飾ブロック・建築バリエーション・ランタン)。
2. **新エネルギーシステム**: 「Prismium Energy」。FE(Forge Energy)ベース。発電機(Generator)・蓄電ブロック(Cell)・3機械(Pulverizer/Smelter/Compressor、共通基底クラス抽出済み)まで実装済み。ケーブル(離れたブロック間の中継)・GUI連携・複数ブロックにまたがる大規模送電網はまだ無い。
3. **新ディメンション**: 「Prism Realm」。`minecraft:flat`ジェネレータによる水没ワールド(陸地なし、専用バイオーム`claudemod:prism_realm`、深層岩/石境界は自作Perlin/Fractalノイズでまだら化済み)、テレポート用アイテム(Prismium Rift Shard)まで実装済み。プリズミウム鉱石は既にオーバーワールドと共通の鉱石として生成される(Realm側は生成率を通常より高くブースト済み)。陸地(平原等)・Realm専用の鉱石種・本格的なポータルブロックはまだ無い。将来的には各バイオームに固有ボスを伴うダンジョンがまれに生成される仕組みを構想中(山岳地帯版から着手予定、TODO参照)。
4. **新MOB**: Prismium Wraith/Deep Wraith(戦闘)、Sentinel(戦闘)、Drifter(水中非戦闘)、Crawler(地上アンビエント、5体目)の5体。ボス級はまだ無いが、各バイオーム固有のダンジョンボスとして今後複数体追加予定(TODO参照)。カテゴリ拡充の余地あり。
5. **新装備**: ツール5種・アーマー4種(セット効果: 暗視+水中呼吸)、グラップリングフック、探知アイテム(Locator)、Shield、Bow、Guardian Charm(cheat-death)、Featherstone/Emberguard/Vitastone/Magnet Charm(完全パッシブ系)まで実装済み。一部はCuriosAPI対応済み(Issue #18)。
6. **新ブロック/ギミック**: Prismium Core、Prismium Lantern、蒼白のプリズミウムブロック・ランタン・建築バリエーションまで実装済み。装飾ブロック・罠・ダンジョン用ギミックブロックはさらに拡充の余地がある。
7. **プレイヤー向けUX/ドキュメンテーション**: issue #7(MODについて何も分からない)への対応として、各エネルギーブロックの使い方ツールチップ(session 38)、詳細表示オーバーレイ(W長押し)、そしてv0.32.0のプリズミウム・コンペンディウム(初回配布されるガイドブック)まで実装済み。今後も新しい系統(Prism Realmのダンジョン/ボス等)を追加するたびに、この系統のドキュメントも一緒に更新していく。
